pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh '''
                #!/bin/sh
                echo "Hello World"
                '''
            }
        }
    }
}
